 函数的参数
1.参数扩展/数组展开
2.默认参数

参数扩展作用：
1.收集剩余参数
 function show(a,b,...args) {}
 *Rest Parameter必须是最后一个形参
 2.展开数组(...arr===1,2,3)
 展开后的效果，跟直接把数组的内容写在这一样


 -----------------------------------

 默认参数
 就是在参数中直接赋值，传就用传的，没传就用默认值
$('#div1').animate({width:'200px'});
$('#div1').animate({width:'200px'},1000);



---------------------
...args 剩余参数 必须是最后一个形参
args是一个数组
